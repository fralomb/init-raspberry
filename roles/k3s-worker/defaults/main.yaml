---
# Default variables for k3s-worker role

# K3s version to install (should match master)
k3s_version: "v1.29.0+k3s1"

# K3s installation script URL
k3s_install_script_url: "https://get.k3s.io"

# Extra arguments for K3s agent
k3s_worker_extra_args: ""

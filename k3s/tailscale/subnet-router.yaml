---
# Tailnet devices reach the home LAN (and Traefik on the master) through this node.
# Needs the tailscale.com CRDs: k3s retries it until the operator chart installs them.
apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: homelab-subnet-router
spec:
  hostname: homelab-subnet-router
  tags:
    - "tag:k8s"
  subnetRouter:
    advertiseRoutes:
      - "192.168.1.0/24"
